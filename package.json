{
  "name": "eth-pool3",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ sherifahmed990/eth-pool3",
    "create-local": "graph create --node http://localhost:8020/ sherifahmed990/eth-pool3",
    "remove-local": "graph remove --node http://localhost:8020/ sherifahmed990/eth-pool3",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 sherifahmed990/eth-pool3"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.28.1",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
